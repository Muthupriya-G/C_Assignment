#include<stdio.h>
int main(){
	int i=0,j;
	char arr[i];
	printf("Enter the number of characters:");
	scanf("%d",&i);
	printf("Enter the characters:");
	scanf("%s",&arr);
	printf("The characters are:%s",arr);	
}
